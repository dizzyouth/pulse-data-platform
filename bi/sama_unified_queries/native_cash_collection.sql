SELECT
    business_id,
    currency,
    economic_status,
    delivered_orders,
    collected_native_currency,
    cod_fee_native_currency,
    cash_after_cod_fee_native_currency
FROM marts.sama_pilot_unified_native_economics
