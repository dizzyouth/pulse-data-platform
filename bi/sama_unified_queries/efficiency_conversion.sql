SELECT
    business_id,
    confirmation_rate,
    delivery_rate,
    return_rate,
    cost_per_lightfunnels_order_usd,
    cost_per_confirmed_order_usd,
    marketing_cost_per_delivered_order_usd
FROM marts.sama_pilot_unified_overview
